cpan HTTP::Server::Simple::CGI

Model, View, Controller + structure

README.md: Clearly document that wkhtmltopdf is a required dependency and provide instructions for installation.
Environment Variable: Allow users to specify the wkhtmltopdf path via an environment variable, offering flexibility if they want to use their system-installed version.